TankDrainTimeAnalysis
└──Behavior
    └──Pump_Behavior
        └──Pump_Behavior
            └──Fault h <= h_lowlow critical low level
            └──Pump OFF h >= h_high pump idle
            └──Pump ON h <= h_low pump running
    └──Library
    └──FaultStatus
    └──PumpStatus
    └──Area
    └──FlowRate (Q)
    └──Length
    └──Time
    └──Velocity
└──Requirements
    └──Requirements
    └──Pump ON/OFF Thresholds
    └──Fault Detection At Low-Low Level
    └──Drain Time Calculation
    └──Sensor Sampling Interval
    └──Detects critically low water conditions to prevent...
    └──Ensures that control logic receives timely data to...
    └──Ensures the tank maintains safe operating levels, ...
    └──Provides accurate timing for pump control and sens...
└──Structure
    └──WaterSystemMap
    └──DrainPipe
        └──a_pipe : TankDrainTimeAnalysis::Library::Area = 0.05
            └──0.05
        └──in
    └──Pump
        └──status : TankDrainTimeAnalysis::Library::PumpStatus
        └──out
    └──WaterSystem
        └──WaterSystem
        └──waterTank : TankDrainTimeAnalysis::Structure::WaterTank
        └──drainPipe : TankDrainTimeAnalysis::Structure::DrainPipe
        └──pump : TankDrainTimeAnalysis::Structure::Pump
    └──WaterTank
        └──A_tank : TankDrainTimeAnalysis::Library::Area = 2.0
            └──2.0
        └──h_high : TankDrainTimeAnalysis::Library::Length = 5.0
            └──5.0
        └──h_low : TankDrainTimeAnalysis::Library::Length = 1.0
            └──1.0
        └──h_lowlow : TankDrainTimeAnalysis::Library::Length = 0.5
            └──0.5
        └──in
        └──out